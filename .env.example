# Required: Database URL for PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/insyd_notifications?schema=public"

# Optional: Hugging Face API Token for AI features
# If not provided, the app will use a simple heuristic ranking system
HF_API_TOKEN=""

# Next.js environment
NODE_ENV="development" # or "production"
